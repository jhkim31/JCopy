
/**
 * ```javascript
 * {
 *  id: string;
 *  roomId: string;
 *  textId: string;
 *  textValue: string;
 *  clientId: string;
 * }
 * ```
 */
export default interface IChangeText {
    id: string;
    roomId: string;
    textId: string;
    textValue: string;
    clientId: string;
}